# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.next/
qa-output/
*.tsbuildinfo
next-env.d.ts

# Vercel CLI project link (local-only project/org IDs, not shared with the team)
.vercel

# Secrets — never commit
.env
.env.local
.env.*.local

# Runtime-editable content and uploads (admin portal), not source
/data/
/public/uploads/

# Per-machine Claude Code settings, not shared with the team
.claude/settings.local.json

.vercel
.env*
